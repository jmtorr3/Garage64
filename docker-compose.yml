services:
  backend:
    build: ./backend
    volumes:
      - db_data:/app/db.sqlite3
      - pack_data:/pack
    environment:
      - PACK_ROOT=/pack

  frontend:
    build: ./frontend
    ports:
      - "3264:3264"
    depends_on:
      - backend

volumes:
  db_data:
  pack_data:
