services:
  backend:
    build: ./backend
    volumes:
      - db_data:/app/db
      - ./Garage64_LATEST:/pack:ro
    environment:
      - PACK_ROOT=/pack

  frontend:
    build: ./frontend
    ports:
      - "3264:3264"
    depends_on:
      - backend

volumes:
  db_data:
